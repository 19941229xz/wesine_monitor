<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wesine.dao.TransactionMapper">

	<insert id="insert" parameterType="java.util.Map">
		insert into transactions (ShopID,RegID,CashierID,CustomerID,TsStart,TsEnd,TransID,ScriptVer,Priority,id,CounterID)
		values(#{ShopID},#{RegID},#{CashierID},#{CustomerID},#{TsStart},#{TsEnd},#{TransID},#{ScriptVer},#{Priority}
		,#{id},#{CounterID})
	</insert>


</mapper>